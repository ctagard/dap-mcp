run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - gocritic
    - revive
    - bodyclose
    - noctx
    - errorlint
    - gosec
    - prealloc

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: false  # Allow _ = to explicitly ignore errors

  govet:
    enable-all: true
    disable:
      - fieldalignment  # Too strict for this project
      - shadow  # Intentional pattern: if x, err := ...; err == nil

  gofmt:
    simplify: true

  goimports:
    local-prefixes: github.com/ctagard/dap-mcp

  misspell:
    locale: US

  gocritic:
    enabled-tags:
      - diagnostic
      - performance
    disabled-checks:
      - whyNoLint
      - hugeParam
      - rangeValCopy  # Performance optimization not critical
      - appendCombine  # Style preference
      - unnamedResult  # Style preference
      - paramTypeCombine  # Style preference
      - singleCaseSwitch  # Style preference
      - ifElseChain  # Style preference
      - typeAssertChain  # Style preference

  revive:
    rules:
      - name: blank-imports
      - name: context-as-argument
      - name: context-keys-type
      - name: dot-imports
      - name: error-return
      - name: error-strings
      - name: error-naming
      - name: exported
      - name: increment-decrement
      - name: var-naming
      - name: package-comments
      - name: range
      - name: receiver-naming
      - name: time-naming
      - name: unexported-return
      - name: indent-error-flow
      - name: errorf

  gosec:
    excludes:
      - G104  # Unhandled errors - too many false positives
      - G304  # File path from variable - expected in this project

issues:
  exclude-rules:
    # Exclude some linters from running on test files
    - path: _test\.go
      linters:
        - errcheck
        - gosec
        - gocritic

    # Exclude test fixture directories (sample code for debugging tests)
    - path: test/go_project/
      linters:
        - errcheck
        - gocritic
    - path: test/python_project/
      linters:
        - all
    - path: test/c_project/
      linters:
        - all
    - path: test/typescript_project/
      linters:
        - all
    - path: test/react_project/
      linters:
        - all

    # Ignore long lines in generated files
    - path: ".*\\.pb\\.go"
      linters:
        - all

  max-issues-per-linter: 0
  max-same-issues: 0
